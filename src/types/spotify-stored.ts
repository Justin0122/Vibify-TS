export interface User {
    id: number;
    user_id: string;
    access_token: string;
    refresh_token: string;
    expires_in: string;
    api_token: string;
    created_at: string;
    updated_at: string;
    expires_at: Date;
}